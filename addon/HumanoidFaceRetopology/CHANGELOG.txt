CHANGELOG
=========

v1.0.2 - Bundled binding repair path fix
-----------------------------------------

- Fixed the bundled template binding repair path used by installed add-on packages.
- Reload Default Template, Initialize HFR Workspace, and Generate Retopology can now correctly restore the bundled HFRTemplateBinding.json data.

v1.0.2 - Template binding package fix
-------------------------------------

- Fixed the release package so the bundled default template can restore its complete landmark binding data.
- Reload Default Template, Initialize HFR Workspace, and Generate Retopology now recover the bundled template binding when needed.
